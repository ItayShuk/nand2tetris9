Class Snake{
    // represents the head of the snake
    field int xHead;
    field int yHead;
    field Array body;
    field int size;
    field int direction;

    Constructor Snake(int x, int y){
        let xHead = x;
        let yHead = y;
        let body = new Array(1)
        let body[0] = new Points(x,y)
        return this;
    }

    method void moveUp(){

    }

    method void moveDown(){

    }

    method void moveLeft(){

    }

    method void moveRight(){

    }

    method void incSize(){
        var Array new_body;
        let new_body = new Array (size+1);
        let i = 0;
        new_body[0] = new Points(xHead+direction.getX(),yHead+direction.getY())
        while (i < length) {
            let new_body[i+1] = body[i];
            let i = i + 1;
        body = new_body;
    }

    method int getXHead(){
        return xHead;
    }
    method int getYHead(){
        return yHead;
    }

}